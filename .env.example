# 根目录环境变量示例（可选）：复制为 `.env` 后按需填写
# 后端实际读取的是 `backend/.env`（见 README）

LLM_PROVIDER=deepseek
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
APP_ENV=development
BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
